{% extends 'base.html.twig' %}

{% block title %}Zadania{% endblock %}

{% block body %}
    <h1>Zadania</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zadanium.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ zadanium.nazwa }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ zadanium.opis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('zadania_index') }}">back to list</a>

    <a href="{{ path('zadania_edit', {'id': zadanium.id}) }}">edit</a>

    {{ include('zadania/_delete_form.html.twig') }}
{% endblock %}
